name: Sync to Hugging Face Space
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Load prod deploy key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_DEPLOY_KEY }}

      - name: Build and deploy prod branch
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          MAIN_SHA=$(git rev-parse --short HEAD)

          # Create clean branch from main, excluding non-deployment files
          git checkout -b prod-build
          git rm -rf scripts/ docs/ .github/
          git commit -m "prod: built from main@${MAIN_SHA}"

          # Push to GitHub prod via SSH; deploy key bypasses prod ruleset (see #31)
          git push git@github.com:${{ github.repository }}.git prod-build:prod --force

          # Push to HF Space (HF expects the main branch)
          git push https://netzhang:$HF_TOKEN@huggingface.co/spaces/imageomics/bioclip-image-search-lite prod-build:main --force
